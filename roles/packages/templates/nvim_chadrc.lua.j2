-- NvChad UI Customization
local M = {}

M.ui = {
  theme = "tokyonight",
  transparency = false,
}

M.plugins = "custom.plugins"

-- Don't override default mappings, use NvChad defaults
-- M.mappings = require "custom.mappings"

-- Enable LSP semantic highlighting for variable/type tagging
-- This ensures variables, functions, types are properly highlighted
M.ui.semantic_tokens = {
  enable = true,
}

return M
